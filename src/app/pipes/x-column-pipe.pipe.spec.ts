import { XColumnPipePipe } from './x-column-pipe.pipe';

describe('XColumnPipePipe', () => {
  it('create an instance', () => {
    const pipe = new XColumnPipePipe();
    expect(pipe).toBeTruthy();
  });
});
