import { ActorImageSelectorPipe } from './actor-image-selector.pipe';

describe('ActorImageSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new ActorImageSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
