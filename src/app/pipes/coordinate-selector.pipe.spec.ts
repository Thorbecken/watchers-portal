import { CoordinateSelectorPipe } from './coordinate-selector.pipe';

describe('CoordinateSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new CoordinateSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
