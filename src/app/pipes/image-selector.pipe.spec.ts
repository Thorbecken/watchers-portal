import { ImageSelectorPipe } from './image-selector.pipe';

describe('ImageSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
